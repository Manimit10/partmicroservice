doctype html
html
  head
    title Create Order
  body
    h1 Create a New Order
    form(action="/order/create" method="POST")
      div
        label(for="items") Items:
        input(type="text" name="items" placeholder="Enter items separated by commas" required)
      button(type="submit") Create Order
